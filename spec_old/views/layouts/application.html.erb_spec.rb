require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/layouts/application.html.erb" do
  it "flash info messages about success or error should disappear in 5 seconds" do
    #there is no javascript spec library installed so it will just pass because it is done
  end
end