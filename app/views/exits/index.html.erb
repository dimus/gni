<h1>Listing exits</h1>

<table>
  <tr>
  </tr>

<% for exit in @exits %>
  <tr>
    <td><%= link_to 'Show', exit %></td>
    <td><%= link_to 'Edit', edit_exit_path(exit) %></td>
    <td><%= link_to 'Destroy', exit, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New exit', new_exit_path %>
